#include<iostream>
#include <stack>

using namespace std;

int main(){
    int n, k, ki;
    cin >> n >> k;
    stack<int> v;

    for(int i = 0; i < n; i++)
        v.push(i);
    
    for(int i = 0; i < k; i++){
        cin >> ki;

        for(int j = 0; j < ki; ki++){
            if()
        }
            v.pop;
    }
}